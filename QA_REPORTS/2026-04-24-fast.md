# QA Report — 2026-04-24 — fast

## Summary

Codebase is in good shape. All five checks pass or produce only known-and-deferred issues. The two hardcoded owner names are the only open items and both are explicitly deferred to Phase E (Workspace multi-tenancy). Nothing blocks the next push or the pizazz pass.

## Findings

| # | Check | Status | Note |
|---|-------|--------|------|
| 1 | TypeScript | PASS | `npx tsc --noEmit` — zero errors |
| 2 | ESLint | PASS | 0 errors, 17 warnings — all pre-existing (unused vars, img elements, missing deps); none block CI |
| 3 | Auth guard audit | PASS | All 40+ handlers checked. `parse-document` returns 422 with no data access (legitimately unguarded). `auth/join` and all write routes have `getUser()` + 401 guard. |
| 4 | Property scoping audit | PASS | All `[id]` routes (rooms, visual-config, assets, tasks, projects, purchases, budget-lines, etc.) include `.eq('property_id', propertyId)` on both read and write. New `rooms/[id]` PATCH/DELETE double-scoped correctly. |
| 5 | Restricted zone compliance | PASS | Latest commit ("Parcel refinement: 2D visual system, rooms, PDF fix, suggestions fix") adds migrations 026 and 027. Both are new tables (not schema changes to existing tables) with correct RLS patterns documented in session notes 2026-04-23c. No auth or permission logic altered without rationale. |
| 6 | Known-issue scan | WARN | Two hardcoded owner name instances found — see below. No TODO/FIXME/HACK comments. No hardcoded coordinates. |

## Issues requiring action

**WARN — `app/welcome/page.tsx:44`**
Hardcoded "Hi Brady" in the demo request email template. Will expose the owner's name to any user who visits the welcome page.
Fix: replace with a generic placeholder or make it configurable via env var.

**WARN — `lib/agent/system-prompt.ts:93`**
Hardcoded "Brady and Erin" in the Pattern awareness paragraph of the system prompt. All tenants using this product will get an agent that references these names.
Fix: derive owner names dynamically from the property membership record or omit the names entirely.

## Deferred

Both WARN items above are intentionally deferred to Phase E (Workspace / multi-tenancy). Session notes 2026-04-23c and CLAUDE.md confirm `suggestions/route.ts` name hardcoding is also tracked as a Phase E concern. No new issues found.
