# QA Report — 2026-04-25 — fast

## Summary

Codebase is clean. TypeScript and lint pass with zero errors. All 45 API routes have proper auth guards; all write operations on dynamic [id] routes are property-scoped. The three hardcoded owner name instances are pre-existing, known, and deferred to Phase E. Parcel Z and Phase C work (C1, C2, Z) can be considered stable going into C3.

## Findings

| # | Check | Status | Note |
|---|-------|--------|------|
| 1a | TypeScript | PASS | `tsc --noEmit` clean |
| 1b | Lint | PASS | 0 errors; 13 warnings (all pre-existing, none in new Parcel Z files) |
| 2 | Auth guard audit | PASS | 45 routes checked; `parse-document` has no guard but is a static 422 stub with no data access — acceptable |
| 3 | Property scoping | PASS | All dynamic [id] writes confirmed: `.eq('property_id', PROPERTY_ID)` or `.eq('lattice_id', latticeId)` present on every update/delete |
| 4 | Restricted zone compliance | PASS | Migrations 030 and 029 both have session notes with rationale; no auth or RLS touched without documentation |
| 5 | Known-issue scan | WARN | 3 hardcoded owner name instances — see below |

## Issues requiring action

**5a — Hardcoded contact email** `app/welcome/page.tsx:41`
`brdesko@gmail.com` is hardcoded as the request-access contact. Will surface to real users if the product ever opens. Fix in Phase E (multi-tenancy pass).

**5b — Hardcoded name in email template** `app/welcome/page.tsx:44`
"Hi Brady" hardcoded in the request-access mailto body. Same Phase E fix.

**5c — Hardcoded names in agent system prompt** `lib/agent/system-prompt.ts:93`
"Brady and Erin" appear in the pattern awareness paragraph. The agent references these names in its reasoning about behavioral patterns. Acceptable for single-user operation; becomes incorrect when the product is multi-tenant. Phase E.

## Deferred

- `app/welcome/page.tsx` hardcoded owner name/email — Phase E multi-tenancy, per PLAN.md and session notes
- `lib/agent/system-prompt.ts` hardcoded names — Phase E, same rationale
- 13 lint warnings (unused vars, `<img>` vs `<Image />`, useEffect deps) — pre-existing, not introduced by Parcel Z or Phase C work; tracked but not blocking
- zone_id/space_id assignment UI on projects/tasks/assets — FKs in place, UI deferred per 2026-04-25c session notes
