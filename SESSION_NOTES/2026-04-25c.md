# Session Notes — 2026-04-25c

## What we did

### Parcel Z complete — Zone/Space infrastructure

**Migration 030** (`supabase/migrations/030_parcel_z.sql`) — ready to run in Supabase:

**`zones` table** — replaces JSONB zones in `property_visual_config.site_config`:
- `name`, `color`, `x`, `y`, `width`, `height`, `description`, `floor_plan_photo_url`, `sort_order`
- Owner-only write RLS via property membership pattern
- Existing site_config rows have zones stripped via JSONB operator (`- 'zones'`)

**`spaces` table** — replaces `rooms`:
- `zone_id uuid` is a real FK to `zones(id)` (was `text` referencing JSONB keys)
- Same status/notes/pos_* shape as rooms
- Cascades on zone delete

**FK columns added:**
- `projects`, `tasks`, `assets`, `ongoing_tasks` all get nullable `zone_id` + `space_id` FKs
- `on delete set null` — no cascade, just detach if zone/space is removed

**`ongoing_task_instances` table:**
- One row per task per year (`unique (ongoing_task_id, year)`)
- `status`: pending / complete / skipped
- `completed_at` timestamptz
- Owner-only write RLS

**API routes** (committed `f029388`):
- `GET/POST /api/zones`
- `PATCH/DELETE /api/zones/[id]`
- `GET/POST /api/spaces` (optional `?zone_id=` filter)
- `PATCH/DELETE /api/spaces/[id]`
- `/api/rooms` routes deleted

**Agent tools updated:**
- `get_rooms` / `manage_room` removed
- Added: `get_zones`, `manage_zone` (create/update/delete zones in DB)
- Added: `get_spaces`, `manage_space` (replaces manage_room, zone_id is now UUID)
- `derive_visual_from_photo` updated: deletes existing zones, inserts derived zones to `zones` table, saves `{ bounds, buildings }` to site_config (no zones in JSONB)
- `update_visual_config` description updated: bounds/buildings only, manage_zone for zone edits

**Components updated:**
- `site-plan.tsx`: `SiteConfig` no longer includes `zones[]`; `SitePlan` component now accepts `zones: Zone[]` as a separate prop
- `zone-interior.tsx`: `Room` → `Space`, all `/api/rooms` calls → `/api/spaces`
- `index.tsx` (PropertyVisual): fetches zones from `/api/zones` + spaces from `/api/spaces`; all Room → Space references updated; `hasConfig` now based on `zones.length > 0`

**System prompt updated:** rooms → spaces, visual plan workflow updated to explain zones are in DB and managed via manage_zone.

TypeScript clean, build clean.

---

## State at end of session

- Migration 030 written but NOT yet run in Supabase — needs to be applied
- Parcel Z complete in code
- TypeScript clean, build clean
- Commit `f029388`

---

## Next session starting point

**Run migration 030 in Supabase** before testing the Visual tab.

After that, next planned work is **C3 — Shared planning model**: how domains publish into the Global Context finances, time, and goal systems.

Outstanding deferred items (still tracking):
- Hardcoded owner names in welcome page and system prompt — Phase E
- Visual tab continual improvement — parked
- Existing production `lattice_id = null` on properties — bootstrap in C4 shell
- zone_id/space_id linkage UI on projects/tasks/assets — deferred; FKs are in place but no assignment UI yet
