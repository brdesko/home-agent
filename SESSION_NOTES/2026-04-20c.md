# Session Notes — 2026-04-20 (third session)

## What we built

Phase 2, Slices 1 and 2 — complete and deployed.

### Pre-work
- Added `/review` slash command in `.claude/commands/review.md` — audits changes against CLAUDE.md principles
- Added warn-before-long-tasks rule to CLAUDE.md and persistent memory

### Phase 2 Slice 1 — Agent with add-project capability
- Installed `@anthropic-ai/sdk`
- `/agent` page: server component for auth, client `AgentChat` component with greeter, message history, loading state, project-created confirmation banner
- `/api/agent` route: Claude tool-use loop with `create_project` tool, prompt caching on system prompt, iteration cap (10) to prevent infinite loops
- Agent link added to Notebook header
- Tested: "add a home gym project in Barn 2" — Agent asked clarifying questions, proposed, waited for approval, committed

### Phase 2 Slice 2 — Expanded Agent (tasks + budget + timeline)
- Expanded `create_project` tool to also accept `tasks`, `budget_lines`, and `timeline_events`
- Updated system prompt: Agent now proposes a complete project package using judgment, not by asking the user to enumerate every item
- Updated confirmation banner to show task count, estimated budget, and timeline event count
- Tested: kitchen renovation — Agent asked focused clarifying questions, proposed full line-item list, committed everything

### Deployment
- Added `ANTHROPIC_API_KEY` to Vercel environment variables
- Pushed to GitHub; Vercel auto-deployed
- Verified: Notebook and Agent both working on production URL

## Decisions made

- **Skipped PROPERTY_ID hardcode fix** — only one property in use for the foreseeable future; cleanup deferred until a second property is actually needed
- **Single `create_project` tool handles everything** — simpler than multiple tools for Phase 2; Claude passes tasks/budget/events as arrays in one call
- **Agent uses judgment on tasks** — system prompt instructs Claude to propose a sensible starting set of tasks rather than asking the user to enumerate them

## Open items going into Phase 3

- PROPERTY_ID is hardcoded in `/api/agent/route.ts` — needs to be derived from session when multi-property is enabled
- Home gym project from Slice 1 has no tasks or budget (created before the expanded tool) — could be fleshed out via Agent once modify capability exists
- No streaming yet — responses appear all at once; can add in Phase 3 for better feel
- Seed data in `5090-durham.json` is a starting point; real content still needs to be refined

## Next step

Phase 3 — The Consultant: Agent gains ability to modify existing projects, respond to disruptions, and suggest proactively.
