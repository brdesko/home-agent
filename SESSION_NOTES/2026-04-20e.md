# Session Notes — 2026-04-20e (Phase 3 close)

## What we built

### To-Do restructure
- Unified timeline list: This Week + Rest of Q{n} sections
- Three category badges: Suggested (amber), Ongoing (blue), Project (zinc)
- Task expansion: Go to project, Add cost line, Ask Agent (pre-loaded context via `?ctx=`)
- Category filter pills + per-section progress bars
- Fixed "Ask Agent" spuriously showing task as completed
- migration 016: `ongoing_tasks` table with `active_months[]`

### Home Details page (`/home-details`)
- Details tab: editable property fields, save-on-blur
- Documents tab: Supabase Storage upload/list/download/delete + "Parse with Agent"
- Assets tab: full CRUD (make/model/serial/install date/last serviced/location/notes), slide-over panel
- Photos tab: stub

### Agent document parsing
- `/api/agent/parse`: handles PDF, images, plain text, pasted text
- Confirmation modal: property details / assets / suggested projects (each checkboxable)
- Projects come with pre-suggested 2–5 starter tasks, individually checkboxable
- Robust JSON extraction (handles markdown code fences from Claude)
- Date sanitization + numeric coercion; per-item errors at top of modal (non-blocking)
- Zillow/Redfin block URL scraping — paste-text mode works; Firecrawl parked

### Suggestions enhancement
- Assets + property details now included in suggestions API context
- Agent can flag overdue maintenance, aging systems

### Other
- Tab order changed: To-Do → Dashboard → Project Management (default: To-Do)
- migrations 017–019: property detail columns, extended asset columns, Storage RLS
- New API routes: ongoing-tasks, property, assets, documents, agent/parse, tasks

## Decisions
- Suggested projects from parse: `status: 'planned'`, appear in Project Management
- Parse errors are non-blocking per-item

## Parking lot
1. Firecrawl — JS-rendered URL crawling + screenshots
2. Hardcoded `PROPERTY_ID` — must become per-user lookup
3. Visual/zones — satellite + floor plan map
4. Onboarding wizard — Zillow URL → agent → first goal

## Phase 4 goals (next session)
- UI aesthetics and polish
- Maneuverability: create projects/tasks from UI, URL state, keyboard shortcuts
- Practice with a real new project end-to-end
- Extend access to Erin; plan Viewer role for parents
