# Session Notes — 2026-04-20 (second session)

## What we built

Phase 1, Slices 2 and 3 — complete.

### Slice 2 — Core data model
- Migration 006: `projects`, `tasks`, `assets`, `budget_lines`, `timeline_events` tables with full RLS
- RLS pattern: members read most tables; owners read/write everything; `budget_lines` is owners-only even for reads
- Seed data approach: JSON + TypeScript loader script (not SQL), so content is easy to edit and the pattern anticipates the Agent
- `supabase/seed/5090-durham.json`: 10 projects across 5 domains, 48 tasks, 19 budget lines, 8 assets, 6 timeline events
- `scripts/seed-property.ts` + `npm run seed:property` — idempotent upsert, bypasses RLS via service-role key

### Slice 3 — Notebook UI
- `app/page.tsx` replaced with full Notebook: projects grouped by domain, two-column layout
- `components/notebook/project-card.tsx`: title, status badge, priority dot, description, task list, budget total
- `components/notebook/timeline-panel.tsx`: upcoming events sidebar
- `components/notebook/budget-panel.tsx`: per-project estimated totals, grand total (owners only)
- `app/layout.tsx`: updated metadata

## Decisions made

- **Seed content as JSON, not SQL.** Schema stays in SQL migrations; domain content lives in JSON with a loader. This is the pattern the Agent will use in Phase 2.
- **Seed content extrapolated from user's rough notes.** User provided a description of in-progress and planned work; Claude fleshed it out into structured projects/tasks. Intended as a starting point for modification, not a complete record.
- **"Full data visible" exit criterion is about wiring, not completeness.** The Notebook displays real data end-to-end. Richer content import is Phase 2+ work.
- **Warn before long tasks added to CLAUDE.md and memory.** User corrected this three times in one session; now enforced as a documented rule.

## Phase 1 exit criteria — all met

- Brady can sign in as Owner ✓
- Erin can sign in as Owner ✓
- Full property data visible in Notebook UI ✓
- Second domain stub exists (Kitchen Renovation project in seed) ✓
- RLS verified: random user UUID sees 0 rows across all tables ✓

## Open items going into Phase 2

- Seed content needs refinement — the JSON is a starting point, not ground truth
- No self-serve password reset flow for Brady or Erin
- Vercel deployment not yet updated with migration 006 (tables exist in Supabase but Vercel preview URL still points to old page)
- Aesthetic direction deferred — revisit when building Phase 2 UI
- Phase 2 Slice 1 steps to be planned at start of next session

## Next step

Phase 2, Slice 1: Agent with one capability — "add a new project" via conversation.
